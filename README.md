# jahia-cli pitch (archived)

An early sketch for a shared `jahia` command: one CLI that gathers what each team
already does around Jahia (spin up a local environment, deploy a module, run a
provisioning manifest), extensible by plugins so every team can add its own.

This proposal is being taken forward elsewhere. The PR is kept closed and nothing
here is meant to be merged.
